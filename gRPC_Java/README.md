# Taak 4: gRPC

Deze gRPC werkt via een bidirectional stream (als uitbreiding). De client stuur voetbalspelers naar de server, de server berekent of deze gewisseld moeten worden, en stuurt de gegevens terug naar de client.

## Om te runnen
In de root van dit project: ```docker compose up --build```